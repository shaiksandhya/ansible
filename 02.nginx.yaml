- name: Install nginx and run
  host: mongodb
  become: yes #this will gone root access
  tasks:
   - name: Install nginx
     ansible: builtin.yum:
      name : nginx
      state : installed
   - name : Start nginx
     ansible: builtin.service:
      name: nginx
      state: started    
